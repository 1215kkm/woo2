const products=[
{id:'shell',name:'볼륨 후드 쉘 재킷',en:'VOLUME SHELL 01',cat:'아우터',price:129000,color:'Chalk',desc:'가볍게 걸쳐도 형태가 살아나는 여유로운 실루엣. 넓은 후드와 곡선형 소매가 일상의 움직임을 부드럽게 감쌉니다.',note:'지퍼를 열어 티셔츠 위에 걸치거나, 끝까지 잠가 후드의 입체적인 형태를 즐겨보세요.'},
{id:'blazer',name:'릴랙스 싱글 블레이저',en:'RELAXED BLAZER 02',cat:'아우터',price:148000,color:'Charcoal',desc:'어깨의 힘을 덜어낸 싱글 블레이저. 단정한 라펠과 넉넉한 품이 격식과 편안함 사이의 균형을 잡습니다.',note:'화이트 티셔츠와 함께 입으면 출근하는 날에도, 전시를 보러 가는 주말에도 자연스럽습니다.'},
{id:'shirt',name:'에센셜 드레이프 셔츠',en:'DRAPE SHIRT 03',cat:'셔츠',price:69000,color:'Stone',desc:'차분한 스톤 컬러와 자연스럽게 떨어지는 선. 단독으로도, 얇은 겉옷처럼 열어 입어도 좋은 긴소매 셔츠입니다.',note:'소매를 가볍게 걷고 검은 데님에 매치해 단정한 인상을 완성하세요.'},
{id:'knit',name:'소프트 크루넥 니트',en:'SOFT KNIT 04',cat:'니트',price:79000,color:'Espresso',desc:'깊은 브라운이 만드는 따뜻한 분위기. 단정한 라운드 네크라인과 여유로운 몸판으로 일상에 조용한 깊이를 더합니다.',note:'스톤 톤 팬츠나 짙은 데님과 함께 컬러의 농도를 다르게 조합해보세요.'},
{id:'trouser',name:'와이드 턱 트라우저',en:'WIDE TROUSER 05',cat:'팬츠',price:89000,color:'Charcoal',desc:'허리에서 밑단까지 여유롭게 이어지는 직선. 앞면의 턱이 입체적인 볼륨을 만들어주는 차콜 트라우저입니다.',note:'짧은 재킷과 매치하면 허리선이 드러나면서 와이드 실루엣이 더욱 선명해집니다.'},
{id:'denim',name:'페이드 스트레이트 데님',en:'FADED DENIM 06',cat:'팬츠',price:85000,color:'Washed black',desc:'자연스럽게 바랜 블랙 톤의 스트레이트 데님. 과장 없는 워싱과 곧게 떨어지는 라인이 옷장의 여러 상의와 어울립니다.',note:'밑단을 한 번 접어 신발을 드러내거나, 그대로 길게 내려 편안한 분위기를 연출하세요.'},
{id:'tee',name:'시그니처 박스 티셔츠',en:'BOX TEE 07',cat:'티셔츠',price:39000,color:'Off white',desc:'한 장으로 입어도 균형이 잡히는 박스 형태. 오프화이트 컬러와 정돈된 네크라인이 다양한 레이어링의 출발점이 됩니다.',note:'블레이저 안에는 깔끔하게, 데님 위에는 자연스럽게 꺼내 입어보세요.'},
{id:'bag',name:'데일리 크로스백',en:'DAILY CROSSBODY 08',cat:'액세서리',price:59000,color:'Black',desc:'필요한 것만 가볍게 지니는 날을 위한 크로스백. 간결한 외형과 조절 가능한 스트랩으로 착장의 선을 해치지 않습니다.',note:'스트랩을 짧게 조절하면 재킷 위에서도 몸 가까이 단정하게 착용할 수 있습니다.'}
];
const $=s=>document.querySelector(s),money=n=>'₩'+n.toLocaleString('ko-KR'),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let state;try{state=JSON.parse(localStorage.getItem('forme-demo-v1'))||{}}catch{state={}}state={saved:[],cart:[],orders:[],reviews:[],coupon:false,settings:{},...state};let chosen='',qty=1,couponOn=false;
const persist=()=>localStorage.setItem('forme-demo-v1',JSON.stringify(state));const pget=id=>products.find(p=>p.id===id);const sizes=p=>p.id==='bag'?['ONE']:['S','M','L'];
const icons={search:'<circle cx="10" cy="10" r="6.5"/><path d="m15 15 6 6"/>',bag:'<path d="M5 7h14l1 15H4L5 7Z"/><path d="M8 8V5a4 4 0 0 1 8 0v3"/>',heart:'<path d="M12 21 3 12C-3 4 8-1 12 6c4-7 15-2 9 6Z"/>',user:'<circle cx="12" cy="7" r="4"/><path d="M4 22v-3a8 8 0 0 1 16 0v3"/>',grid:'<path d="M3 3h6v6H3zM15 3h6v6h-6zM3 15h6v6H3zM15 15h6v6h-6z"/>',home:'<path d="m2 11 10-9 10 9M5 9v13h14V9M10 22v-8h4v8"/>',arrow:'<path d="M3 12h18m-7-7 7 7-7 7"/>',close:'<path d="m5 5 14 14M19 5 5 19"/>'};
const icon=n=>`<span class="icon"><svg viewBox="0 0 24 24" aria-hidden="true">${icons[n]||icons.arrow}</svg></span>`;
const routeFiles={"":"index.html","shop":"list.html","search":"search.html","product":"detail.html","cart":"basket.html","checkout":"orderform.html","saved":"wish_list.html","my":"myshop.html","orders":"order_list.html","order":"order_detail.html","complete":"order_result.html","support":"support.html","support/contact":"contact.html","support/notice":"notice.html","events":"events.html","events/welcome":"event-detail.html","coupons":"coupon.html","settings":"settings.html","notifications":"notifications.html","about":"brand-story.html","editorial":"editorial.html","review":"review-write.html","states":"states.html","states/loading":"loading.html","states/error":"error.html","done/contact":"contact-result.html","privacy":"privacy.html"};
function routeUrl(r){let [path,query]=r.split('?');let [base,id]=path.split('/');let file=routeFiles[path]||routeFiles[base]||'error.html';let p=new URLSearchParams(query||'');if(id&&!routeFiles[path])p.set('id',id);return file+(p.size?'?'+p.toString():'')}
function currentRoute(){if(location.hash.startsWith('#/'))return location.hash.slice(2);let route=document.body.dataset.route||'';let q=new URLSearchParams(location.search);if(q.has('id')){route+='/'+q.get('id');q.delete('id')}return route+(q.size?'?'+q:'')}
function go(r){location.href=routeUrl(r)}
const link=(r,t,c='')=>`<a class="${c}" href="${routeUrl(r)}">${t}</a>`;const btn=(t,act,c='')=>`<button class="btn ${c}" data-action="${act}">${t}</button>`;const photo=(p,c='')=>`<img class="${c}" src="images/${p.id}.png" alt="${esc(p.color+' '+p.name)}" loading="lazy">`;
function header(){return `<header>${link('','FORME<sup>®</sup>','logo')}<nav>${link('shop','SHOP')}${link('editorial','THE EDIT')}${link('about','OUR FORM')}</nav><div class="tools">${link('search',icon('search'),'')} ${link('saved',icon('heart'),'desktop')}${link('my',icon('user'),'desktop')}${link('cart',icon('bag')+`<span class="badge">${state.cart.reduce((a,c)=>a+c.qty,0)}</span>`)}</div></header>`}
function footer(){return `<footer><div class="footer-top"><div>${link('','FORME<sup>®</sup>','logo')}<p>덜어낼수록 선명해지는, 나의 형태.</p></div><div class="footer-links"><div>${link('shop','모든 제품')}${link('editorial','에디토리얼')}${link('about','브랜드 이야기')}</div><div>${link('support','고객센터')}${link('events','이벤트')}${link('coupons','쿠폰')}${link('settings','알림·설정')}</div></div></div><div class="footnote"><span>FORME DESIGN STUDY © 2026<br>브랜드·상품·가격·정책·이미지는 디자인 시안입니다. 실제 판매 및 결제가 이루어지지 않습니다.</span><span>QUIET FORM. EVERYDAY FREEDOM.<br>${link('states','시안 상태 보기')} · ${link('privacy','개인정보 안내')}</span></div></footer>`}
function mobile(route){return `<div class="mobile-nav">${[['','home','홈'],['shop','grid','탐색'],['saved','heart','저장'],['my','user','마이']].map(([r,i,t])=>link(r,icon(i)+t,route===r?'active':'')).join('')}</div>`}
function card(p){return `<article class="product"><div class="product-photo">${link('product/'+p.id,photo(p))}<span class="no">${String(products.indexOf(p)+1).padStart(2,'0')} / FORME</span><button class="save icon" data-save="${p.id}" aria-label="${p.name} 저장" aria-pressed="${state.saved.includes(p.id)}">${icon('heart')}</button></div>${link('product/'+p.id,`<h3>${p.name}</h3><p>${money(p.price)}</p><small>${p.color}</small>`)}</article>`}
const grid=ps=>`<div class="grid">${ps.map(card).join('')}</div>`;const title=(en,ko,lead='')=>`<span class="eyebrow">${en}</span><h1 class="page-title">${ko}</h1>${lead?`<p class="page-lead">${lead}</p>`:''}`;
const empty=(h,p,r='shop',b='컬렉션 둘러보기')=>`<div class="empty"><span class="eyebrow">A LITTLE ROOM FOR SOMETHING NEW</span><h2>${h}</h2><p>${p}</p>${link(r,b+' ↗','btn')}</div>`;
function home(){return `<section class="hero" data-section="hero"><picture><source media="(max-width:700px)" srcset="images/mobile-v2.png"><img src="images/hero.png" alt="콘크리트 공간에서 아이보리 쉘 재킷을 입은 남성의 전신 착장" fetchpriority="high"></picture><div class="hero-copy"><span class="eyebrow">COLLECTION 01 — AUTUMN 2026</span><h1>Room<br>to be you.</h1><p>정해진 틀 밖으로.<br>나만의 여유를 입는 방식.</p>${link('shop','새로운 컬렉션 만나기 ↗','btn')}</div><div class="hero-meta"><span>FORME / EVERYDAY UNIFORM</span><span>SEOUL, 2026 — A STUDY OF EASE</span><a href="#collection">SCROLL TO EXPLORE ↓</a></div></section><div class="categories">${['전체','아우터','셔츠','니트','팬츠','티셔츠','액세서리'].map(c=>link('shop?cat='+encodeURIComponent(c),c+' ↗')).join('')}</div><section class="section" id="collection" data-section="collection"><div class="section-head"><div><span class="eyebrow">01 / THE EVERYDAY OBJECTS</span><h2>형태는 간결하게.<br>선택은 자유롭게.</h2></div>${link('shop','전체 컬렉션 보기 ↗','under')}</div>${grid(products)}</section><section class="statement"><span class="eyebrow">OUR POINT OF VIEW<br><br>조용하지만, 분명하게.</span><h2>Less noise.<br><em>More of you.</em></h2></section><section class="section look" data-section="look"><figure><img src="images/look.png" alt="차콜 블레이저와 밝은 팬츠를 입은 남성의 갤러리 착장" loading="lazy"><figcaption class="caption"><span>THE EDIT / 01</span><span>평일과 주말 사이</span></figcaption></figure><div><span class="eyebrow">02 / WEAR IT YOUR WAY</span><h2>일상에도,<br>여백이 필요해.</h2><p>조금 느슨한 어깨, 자연스럽게 흐르는 선.<br>나의 속도를 지키는 옷차림을 제안합니다.</p>${['blazer','tee','bag'].map(id=>{let p=pget(id);return link('product/'+id,`${photo(p)}<span><strong>${p.name}</strong><small>${money(p.price)}</small></span>↗`,'look-row')}).join('')}<p class="notice">착장 사진은 스타일링 예시입니다. 밝은 팬츠와 신발은 코디 소품입니다.</p></div></section><section class="section story" data-section="story"><div><span class="eyebrow tag">03 / THE QUIET WARDROBE</span><h2>좋은 옷보다,<br>자주 입는 옷.</h2><p>매일의 선택이 조금 더 쉬워지도록.<br>함께 입기 좋은 색과 간결한 형태를 모았습니다.<br>나의 옷장에 오래 남길 취향을 발견하세요.</p>${link('editorial','FORME 에디토리얼 읽기 ↗','under')}</div><a href="detail.html?id=knit"><img src="images/knit.png" alt="따뜻한 브라운 크루넥 니트" loading="lazy"></a></section><section class="section"><div class="section-head"><div><span class="eyebrow">04 / START WITH THE ESSENTIALS</span><h2>가장 나다운 기본.</h2></div>${link('about','우리의 이야기 ↗','under')}</div>${grid(products.filter(p=>['shirt','denim','tee','bag'].includes(p.id)))}</section>`}
function shop(q,search=false){const cat=q.get('cat')||'전체',term=q.get('q')||'',sort=q.get('sort')||'edit',max=Number(q.get('max')||999999);let ps=products.filter(p=>(cat==='전체'||p.cat===cat)&&p.price<=max&&(!term||(p.name+' '+p.en+' '+p.color+' '+p.cat).toLowerCase().includes(term.toLowerCase())));if(sort==='low')ps.sort((a,b)=>a.price-b.price);if(sort==='high')ps.sort((a,b)=>b.price-a.price);return `<main class="page">${title(search?'FIND YOUR FORM':'COLLECTION 01',search?'무엇을 찾고 있나요?':'The everyday edit.',search?'이름, 카테고리, 색상으로 나만의 옷을 찾아보세요.':'정돈된 실루엣, 자연스러운 움직임. 매일을 위한 8개의 선택.')} ${search?`<form class="searchbox" id="search-form"><input type="search" name="q" value="${esc(term)}" placeholder="재킷, 니트, Charcoal" aria-label="검색어"><button class="btn">검색 ↗</button></form>${!term?`<p>추천 검색어　${['아우터','니트','팬츠'].map(t=>link('search?q='+encodeURIComponent(t),t,'under')).join('　')}</p>`:''}`:''}<form class="toolbar" id="filters"><label>카테고리 <select name="cat">${['전체','아우터','셔츠','니트','팬츠','티셔츠','액세서리'].map(c=>`<option ${c===cat?'selected':''}>${c}</option>`).join('')}</select></label><label>가격 <select name="max">${[[999999,'모든 가격'],[50000,'5만원 이하'],[100000,'10만원 이하'],[150000,'15만원 이하']].map(([v,t])=>`<option value="${v}" ${v===max?'selected':''}>${t}</option>`).join('')}</select></label><label>정렬 <select name="sort"><option value="edit">추천순</option><option value="low" ${sort==='low'?'selected':''}>낮은 가격순</option><option value="high" ${sort==='high'?'selected':''}>높은 가격순</option></select></label><span class="muted">${ps.length}개의 제품</span></form>${ps.length?grid(ps):empty('검색 결과가 없어요.','다른 검색어를 입력하거나 필터 범위를 넓혀보세요.','search','검색 초기화')}</main>`}
function detail(id){let p=pget(id);if(!p)return errorPage();return `<main><div class="detail"><div><div class="detail-art"><span class="detail-annotation">FORME OBJECT / ${p.en}</span><button class="gallery-open" data-image="${p.id}" aria-label="${p.name} 원본 이미지 확대">${photo(p)}</button></div><div class="detail-counter"><span>01 / PRODUCT STUDY</span><span>${p.color} — FRONT VIEW · 확대 ＋</span></div></div><div class="detail-info"><span class="eyebrow">COLLECTION 01 / ${p.cat}</span><h1>${p.name}</h1><div class="price">${money(p.price)}</div><p class="description">${p.desc}</p><div class="option"><div class="option-title"><span>COLOR / ${p.color}</span><span>01 COLOR</span></div><span class="swatch" style="background:${({shell:'#e5e0d6',blazer:'#454540',shirt:'#c7c3b8',knit:'#49362d',trouser:'#454540',denim:'#343431',tee:'#f1eee6',bag:'#252525'})[p.id]}"></span>${p.color}</div><div class="option"><div class="option-title"><span>SIZE / ${chosen||'사이즈를 선택해 주세요'}</span><a href="#size-guide">사이즈 안내 ↗</a></div><div class="choices">${sizes(p).map(s=>`<button class="choice ${chosen===s?'selected':''}" data-size="${s}" aria-pressed="${chosen===s}">${s}</button>`).join('')}</div></div><div class="option"><div class="option-title"><span>QUANTITY</span><span>${money(p.price*qty)}</span></div><div class="quantity"><button data-qty="-1" aria-label="수량 줄이기" ${qty===1?'disabled':''}>−</button><span>${qty}</span><button data-qty="1" aria-label="수량 늘리기" ${qty===10?'disabled':''}>+</button></div></div><div id="option-error" class="error-text" role="alert"></div><div class="buyline"><button class="icon save" data-save="${p.id}" aria-label="상품 저장" aria-pressed="${state.saved.includes(p.id)}">${icon('heart')}</button>${btn('장바구니에 담기　＋','add:'+p.id,'wide')}</div><p class="notice">시안 배송 정책: 10만원 이상 무료배송 · 미만 3,000원<br>생성 이미지와 가격·옵션은 디자인 검토용입니다.</p></div></div><div class="detail-bottom"><div class="tabs"><a href="#description">제품 이야기</a><a href="#size-guide">사이즈·안내</a><a href="#reviews">후기</a></div><section id="description"><span class="eyebrow">DESIGNED FOR YOUR EVERYDAY</span><h2>${p.en}<br>덜어낸 형태, 더해진 여유.</h2><p>${p.desc}</p>${photo(p)}<p>${p.note}</p></section><section id="size-guide"><h2>선택을 위한 안내</h2><details open><summary>핏과 옵션</summary><p>${p.id==='bag'?'ONE 단일 옵션으로 구성한 크로스백입니다.':'S · M · L 시안 옵션을 제공합니다. 여유 있는 실루엣을 기준으로 디자인했습니다.'} 실제 실측표와 소재 혼용률은 운영 상품 확정 후 등록됩니다. 생성 이미지로 실제 치수나 소재를 판단할 수 없습니다.</p></details><details><summary>배송·교환·반품 안내</summary><p>시안에서는 10만원 이상 무료배송, 그 미만 3,000원을 적용합니다. 실제 발송은 진행되지 않습니다. 교환·반품 접수 정책과 주소는 운영 단계에서 확정됩니다.</p></details><details><summary>제품 디테일과 관리</summary><p>${p.note} 실제 제품의 세탁 라벨과 관리 안내는 상품 확정 후 제공됩니다.</p></details></section><section id="reviews"><h2>사용 후 남기는 이야기</h2>${state.reviews.filter(r=>r.id===p.id).map(r=>`<div class="review"><span class="eyebrow">시안 작성 후기 / ${r.rating}점</span><p>${esc(r.text)}</p></div>`).join('')||'<p>아직 작성된 후기가 없습니다. 임의의 평점이나 구매 후기는 표시하지 않습니다.</p>'}${link('review/'+p.id,'시안 후기 작성하기 ↗','under')}</section></div><section class="section"><div class="section-head"><h2>함께 두면 좋은 것들.</h2></div>${grid(products.filter(x=>x.id!==id).slice(0,4))}</section></main>`}
function totals(items,coupon=false){let subtotal=items.reduce((a,c)=>a+pget(c.id).price*c.qty,0),discount=coupon&&state.coupon&&subtotal>=70000?Math.min(10000,Math.floor(subtotal*.1)):0,shipping=subtotal&&subtotal<100000?3000:0;return{subtotal,discount,shipping,total:subtotal-discount+shipping}}
function sums(t){return `<div class="sum-row"><span>상품 금액</span><span>${money(t.subtotal)}</span></div><div class="sum-row"><span>쿠폰 할인</span><span>−${money(t.discount)}</span></div><div class="sum-row"><span>배송비</span><span>${t.shipping?money(t.shipping):'무료'}</span></div><div class="sum-row total"><span>최종 금액</span><strong>${money(t.total)}</strong></div>`}
function cart(){let items=state.cart.filter(c=>c.selected!==false),t=totals(items);return `<main class="page">${title('YOUR SELECTION','Shopping bag.','마음에 든 형태들을 한곳에. 옵션을 확인하고 다음 단계로 이동하세요.')} ${!state.cart.length?empty('장바구니가 비어 있어요.','매일 입고 싶은 옷을 천천히 골라보세요.'): `<div class="commerce"><div><label class="check"><input type="checkbox" id="select-all" ${items.length===state.cart.length?'checked':''}>전체 선택 (${items.length}/${state.cart.length})</label>${state.cart.map((c,i)=>{let p=pget(c.id);return `<div class="cart-row"><input type="checkbox" data-select="${i}" aria-label="${p.name} 주문 선택" ${c.selected!==false?'checked':''}>${link('product/'+p.id,photo(p))}<div class="item-info"><h3>${link('product/'+p.id,p.name)}</h3><p>${money(p.price)} · ${p.color}</p><div class="cart-controls"><select data-cart-size="${i}" aria-label="${p.name} 사이즈">${sizes(p).map(s=>`<option ${s===c.size?'selected':''}>${s}</option>`).join('')}</select><div class="quantity"><button data-cart-qty="${i}:-1" aria-label="수량 줄이기" ${c.qty===1?'disabled':''}>−</button><span>${c.qty}</span><button data-cart-qty="${i}:1" aria-label="수량 늘리기" ${c.qty===10?'disabled':''}>+</button></div></div><p>${money(p.price*c.qty)}</p></div><button class="icon" data-remove="${i}" aria-label="${p.name} 삭제">×</button></div>`}).join('')}</div><aside class="summary"><h2>주문 예상 금액</h2>${sums(t)}${btn('선택 상품 주문하기 ↗','checkout','wide')}<p class="notice">쿠폰은 주문서에서 적용할 수 있습니다.<br>10만원 이상 주문 시 무료배송 (할인 전 상품 금액 기준)</p></aside></div>`}</main>`}
function checkout(){let items=state.cart.filter(c=>c.selected!==false);if(!items.length)return `<main class="page">${empty('주문할 상품이 없어요.','장바구니에서 주문할 상품을 선택해 주세요.','cart','장바구니로')}</main>`;let t=totals(items,couponOn);return `<main class="page">${title('CHECKOUT / DEMO','Order details.','시안 주문입니다. 실제 개인정보 대신 예시 정보를 입력해 주세요.')}<form id="order-form" class="commerce"><div><h2>배송 정보</h2><div class="form-grid"><label class="field">받는 분<input name="name" required autocomplete="off" placeholder="홍길동" maxlength="30"></label><label class="field">연락처<input name="phone" type="tel" required pattern="[0-9\- ]{9,15}" placeholder="010-0000-0000" autocomplete="off"></label><label class="field full">배송 주소<input name="address" required minlength="5" placeholder="서울시 예시구 예시로 10" autocomplete="off"></label><label class="field full">상세 주소<input name="address2" placeholder="101동 101호 (선택)" autocomplete="off"></label><label class="field full">배송 요청사항<select name="memo"><option>문 앞에 놓아주세요</option><option>직접 받겠습니다</option><option>배송 전 연락해 주세요</option></select></label></div><div class="panel"><h2>주문 상품 ${items.length}개</h2>${items.map(c=>`<div class="sum-row"><span>${pget(c.id).name} / ${c.size} × ${c.qty}</span><span>${money(pget(c.id).price*c.qty)}</span></div>`).join('')}</div><div class="panel"><h2>쿠폰</h2>${state.coupon?`<label class="check"><input type="checkbox" id="apply-coupon" ${couponOn?'checked':''} ${t.subtotal<70000?'disabled':''}>웰컴 10% 쿠폰 적용</label><p class="notice">7만원 이상 · 최대 1만원 할인 · 시안 주문당 1회 적용</p>`:link('coupons','웰컴 쿠폰 받으러 가기 ↗','under')}</div><div class="panel"><h2>결제 방식</h2><p>시안 결제 — 실제 결제나 외부 전송 없이 주문 화면을 체험합니다.</p><label class="check"><input required type="checkbox" name="agree">주문 내용을 확인했으며 시안 주문에 동의합니다.</label></div></div><aside class="summary"><h2>최종 주문 금액</h2><div id="order-totals">${sums(t)}</div><button class="btn wide accent" type="submit">시안 주문 완료하기 ↗</button><p class="notice">주소와 연락처는 저장하지 않습니다.<br>상품·금액·수령인 이름만 이 브라우저에 저장됩니다.</p></aside></form></main>`}
function orders(id,complete=false){let o=state.orders.find(x=>x.id===id);if(id&&!o)return errorPage();return `<main class="page">${title(complete?'THANK YOU / DEMO':'YOUR ARCHIVE',complete?'선택이 완성됐어요.':id?'주문 상세':'주문 내역',complete?'실제 결제와 배송은 진행되지 않는 시안 주문입니다.':'이 브라우저에서 완료한 시안 주문을 확인할 수 있습니다.')}${o?`<div class="panel"><span class="eyebrow">${esc(o.id)} / ${esc(o.date)}</span><h2>${esc(o.name)}님의 시안 주문</h2><p>시안 주문 완료 · 실제 결제 없음</p>${o.items.map(c=>`<div class="cart-row">${photo(pget(c.id))}<div><h3>${pget(c.id).name}</h3><p>${c.size} / ${c.qty}개 · ${money(pget(c.id).price*c.qty)}</p>${link('review/'+c.id,'후기 작성 ↗','under')}</div></div>`).join('')}<div style="max-width:440px;margin-left:auto">${sums(o.totals)}</div></div>${link('orders','주문 내역 보기 ↗','btn light')}`:state.orders.length?state.orders.map(o=>link('order/'+o.id,`<span>${o.date}<br><br>${o.items.map(c=>pget(c.id).name).join(', ')}</span><span>${money(o.totals.total)} ↗</span>`,'coupon')).join(''):empty('아직 주문 내역이 없어요.','시안 주문을 완료하면 이곳에서 확인할 수 있습니다.')}</main>`}
function support(sub){return `<main class="page">${title('HERE TO HELP',sub==='contact'?'무엇을 도와드릴까요?':sub==='notice'?'공지사항':'고객센터','궁금한 점은 천천히 살펴보세요.')}<div class="tabs">${link('support','자주 묻는 질문')}${link('support/notice','공지사항')}${link('support/contact','1:1 문의')}</div>${sub==='contact'?`<form id="contact-form" style="max-width:650px;margin-top:35px"><label class="field">문의 유형<select name="type"><option>상품</option><option>주문·배송</option><option>교환·반품</option><option>기타</option></select></label><label class="field">제목<input required maxlength="80" name="subject"></label><label class="field">내용<textarea required minlength="5" name="body" placeholder="개인정보 없이 시안 문의 내용을 입력하세요."></textarea></label><p class="notice">문의는 외부로 전송되지 않습니다. 제출 완료 화면만 체험합니다.</p><button class="btn">시안 문의 제출 ↗</button></form>`:sub==='notice'?`<details open><summary>2026.09.18　FORME 시안 컬렉션 안내</summary><p>FORME는 남성의류 웹 디자인 시안입니다. 모든 상품 이미지, 가격, 프로모션은 검토용으로 제작되었으며 실제 판매하지 않습니다.</p></details><details><summary>시안 주문과 저장 데이터 안내</summary><p>찜, 장바구니, 주문내역은 현재 브라우저에 저장됩니다. 설정에서 시안 기록을 초기화할 수 있습니다.</p></details>`:[['배송비는 얼마인가요?','시안 주문은 할인 전 상품 합계 10만원 이상 무료배송, 미만 3,000원입니다. 실제 배송은 진행하지 않습니다.'],['사이즈와 소재를 확인하고 싶어요.','상세페이지에서 시안 옵션을 확인할 수 있습니다. 실측과 소재 혼용률은 실제 운영 상품이 확정되면 제공됩니다.'],['쿠폰은 어떻게 사용하나요?','쿠폰 페이지에서 받은 후 주문서에서 적용하세요. 7만원 이상 구매 시 10%, 최대 1만원 할인됩니다.'],['교환·반품을 신청하고 싶어요.','현재는 실제 구매가 없는 시안입니다. 1:1 문의 화면에서 문의 입력과 완료 상태를 체험할 수 있습니다.']].map(([a,b])=>`<details><summary>${a}</summary><p>${b}</p></details>`).join('')}</main>`}
function events(id){return `<main class="page">${title('A GOOD BEGINNING',id?'첫 만남의 여유.':'Events.','FORME를 만나는 작은 계기. 모든 혜택은 시안입니다.')}<div class="event"><img src="images/shirt.png" alt="스톤 컬러 셔츠"><div><span class="eyebrow">WELCOME TO FORME</span><h2>새로운 일상,<br>가벼운 시작.</h2><p>첫 취향을 위한 웰컴 10% 쿠폰.</p><p class="notice">7만원 이상 · 최대 1만원 할인<br>시안 내 상시 제공 / 실제 프로모션 아님</p>${link(id?'coupons':'events/welcome',id?'쿠폰 받으러 가기 ↗':'이벤트 자세히 보기 ↗','btn')}</div></div>${id?'<div class="panel"><h2>이용 안내</h2><p>이 브라우저의 쿠폰함에 저장됩니다. 다른 할인 쿠폰과 중복 적용되지 않으며 배송비에는 할인이 적용되지 않습니다.</p></div>':''}</main>`}
function coupons(){return `<main class="page">${title('A LITTLE EXTRA','My coupons.','취향을 발견하는 시작에 작은 혜택을 더합니다.')}<div class="coupon"><div><span class="eyebrow">WELCOME COUPON / DEMO</span><p><strong>10%</strong>　웰컴 쿠폰</p><p>7만원 이상 · 최대 1만원 할인</p><p class="notice">시안 내 상시 사용 가능 · 배송비 제외 · 주문당 1회 적용</p></div>${state.coupon?'<span>✓ 보유 중</span>':btn('쿠폰 받기 ↓','coupon')}</div>${link('cart','장바구니에서 사용하기 ↗','under')}</main>`}
function my(){return `<main class="page">${title('YOUR OWN FORM','My space.','나의 취향이 쌓이는 곳. 로그인 없는 브라우저 전용 시안입니다.')}<div class="menu-grid">${[['orders','주문 내역',state.orders.length],['saved','저장한 제품',state.saved.length],['coupons','보유 쿠폰',state.coupon?1:0],['notifications','알림','↗'],['settings','설정','↗'],['support','고객센터','↗']].map(([r,n,v])=>link(r,`<span>${n}</span><span>${v}</span>`)).join('')}</div><section class="section-head"><h2>다음 취향을 찾아서.</h2></section>${grid(products.slice(0,4))}</main>`}
function settings(){return `<main class="page">${title('MAKE IT YOURS','Settings.')}<div style="max-width:700px"><div class="panel"><h2>알림 환경</h2>${[['news','컬렉션 소식'],['event','이벤트 소식']].map(([k,n])=>`<label class="check"><input type="checkbox" data-setting="${k}" ${state.settings[k]?'checked':''}>${n} 표시</label>`).join('')}<p class="notice">설정은 브라우저에만 저장되며 실제 푸시 알림은 발송하지 않습니다.</p></div><div class="panel"><h2>시안 데이터 관리</h2><p>저장 상품, 장바구니, 주문과 작성한 후기를 모두 초기화할 수 있습니다.</p>${btn('시안 기록 초기화','reset','light')}</div></div></main>`}
function editorial(about=false){return `<main class="page">${title(about?'OUR FORM / MANIFESTO':'THE EDIT / VOL.01',about?'덜어낼수록, 나답게.':'A slower kind of day.','많은 것보다, 나에게 맞는 몇 가지.')}<section class="look"><figure><img src="images/look.png" alt="여유로운 블레이저 착장"><figcaption class="caption">FORME EDITORIAL / AUTUMN 2026</figcaption></figure><div><span class="eyebrow">QUIET FORM. EVERYDAY FREEDOM.</span><h2>${about?'옷이 앞서지 않고,<br>내가 드러나도록.':'서두르지 않는<br>날의 옷차림.'}</h2><p>FORME는 일상의 형태를 관찰합니다. 출근길의 걸음, 책장을 넘기는 손, 약속 없이 걷는 오후. 자연스러운 순간에 어울리는 옷을 생각합니다.</p><p>어깨의 힘은 조금 빼고, 선은 또렷하게. 차분한 색상 속에서 나만의 조합을 발견하세요.</p>${link('product/blazer','이 장면의 블레이저 보기 ↗','under')}<p class="notice">생성된 스타일링 이미지입니다. 팬츠와 신발은 판매 상품이 아닌 코디 소품입니다.</p></div></section><section class="statement"><span class="eyebrow">OUR THREE PRINCIPLES</span><h2>덜어낸 장식.<br>여유로운 형태.<br><em>자연스러운 일상.</em></h2></section><div class="section-head"><h2>이 이야기와 함께.</h2></div>${grid(products.filter(p=>['blazer','tee','trouser','bag'].includes(p.id)))}</main>`}
function review(id){let p=pget(id);if(!p)return errorPage();return `<main class="page">${title('LEAVE A NOTE','나의 착용 이야기.','실제 구매 후기가 아닌 시안 작성입니다. 이 브라우저에만 저장됩니다.')}<div class="cart-row" style="max-width:650px">${photo(p)}<h3>${p.name}</h3></div><form id="review-form" data-id="${p.id}" style="max-width:650px;margin-top:30px"><label class="field">만족도<select name="rating"><option value="5">5점 · 아주 좋아요</option><option value="4">4점 · 좋아요</option><option value="3">3점 · 보통이에요</option><option value="2">2점 · 아쉬워요</option><option value="1">1점 · 매우 아쉬워요</option></select></label><label class="field">후기 내용<textarea name="text" required minlength="10" maxlength="2000" placeholder="10자 이상 입력해 주세요."></textarea></label><button class="btn">시안 후기 저장 ↗</button></form></main>`}
function errorPage(){return `<main class="page">${title('404 / OUT OF FRAME','잠시, 다른 길에 왔어요.')}${empty('페이지를 찾을 수 없습니다.','주소를 확인하거나 홈으로 돌아가 주세요.','','홈으로 돌아가기')}</main>`}
function states(sub){if(sub==='loading')return `<main class="page" aria-busy="true">${title('LOADING PREVIEW','컬렉션을 준비하고 있어요.')}<p>로딩 상태 디자인 예시입니다.</p><div class="grid">${[1,2,3,4].map(()=>'<div class="skeleton"></div>').join('')}</div>${link('shop','컬렉션으로 이동 ↗','under')}</main>`;if(sub==='error')return errorPage();return `<main class="page">${title('INTERFACE STATES','작은 순간까지.')}<div class="menu-grid">${link('states/loading','로딩 상태 ↗')}${link('states/error','오류 상태 ↗')}${link('search?q=없는제품','검색 결과 없음 ↗')}${link('done/contact','문의 완료 ↗')}</div>${empty('아직 담긴 취향이 없어요.','빈 화면 디자인 예시입니다.')}</main>`}
function render(){const raw=currentRoute(),parts=raw.split('?'),path=parts[0],q=new URLSearchParams(parts[1]||''),[route,id]=path.split('/');document.body.className=route===''?'home':route==='product'?'detail-page':'';let content;switch(route){case'':content=home();break;case'shop':content=shop(q);break;case'search':content=shop(q,true);break;case'product':content=detail(id);break;case'cart':content=cart();break;case'checkout':content=checkout();break;case'saved':content=`<main class="page">${title('KEEP WHAT YOU LOVE','Saved forms.',`${state.saved.length}개의 취향을 모았습니다.`)}${state.saved.length?grid(products.filter(p=>state.saved.includes(p.id))):empty('아직 저장한 제품이 없어요.','마음이 가는 제품의 하트를 눌러보세요.')}</main>`;break;case'my':content=my();break;case'orders':content=orders();break;case'order':content=orders(id);break;case'complete':content=orders(id,true);break;case'support':content=support(id);break;case'events':content=events(id);break;case'coupons':content=coupons();break;case'settings':content=settings();break;case'notifications':content=`<main class="page">${title('STAY IN THE KNOW','Notifications.')}<div class="panel"><span class="eyebrow">서비스 안내 / DEMO</span><h2>FORME에 오신 것을 환영합니다.</h2><p>새로운 컬렉션을 만나고 나만의 취향을 저장해 보세요.</p>${link('settings','알림 설정 ↗','under')}</div>${state.settings.event?`<div class="panel">${link('events/welcome','웰컴 쿠폰 소식 확인하기 ↗')}</div>`:''}</main>`;break;case'about':content=editorial(true);break;case'editorial':content=editorial();break;case'review':content=review(id);break;case'states':content=states(id);break;case'done':content=`<main class="page">${title('COMPLETE / DEMO',id==='contact'?'문의 화면 체험을 마쳤어요.':'저장했습니다.')}<p>시안 동작이 완료되었습니다. 외부로 전송된 내용은 없습니다.</p>${link('support','고객센터로 돌아가기 ↗','btn')}</main>`;break;case'privacy':content=`<main class="page">${title('LOCAL DEMO','개인정보 안내')}<p class="page-lead">이 시안은 회원가입이나 외부 서버 전송을 사용하지 않습니다. 저장 상품, 주문 상품, 수령인 이름과 작성 후기는 현재 브라우저에 보관됩니다. 배송 주소와 연락처는 저장하지 않습니다. 설정에서 기록을 초기화할 수 있습니다.</p></main>`;break;default:content=errorPage()}$('#app').innerHTML=header()+content+footer()+mobile(route)+`<dialog id="image-dialog"><form method="dialog"><button class="icon" aria-label="확대 이미지 닫기">×</button></form><div class="zoom-content"></div></dialog>`;window.initHeroSlider?.();document.title='FORME — '+(route==='product'?(pget(id)?.name||'제품'):route==='shop'?'컬렉션':'여유를 입는 방식');}
let toastTimer;function toast(t){$('#toast').textContent=t;$('#toast').classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').classList.remove('show'),2500)}function redraw(){let y=scrollY;render();scrollTo(0,y)}
document.addEventListener('click',e=>{let anchor=e.target.closest('a[href^="#"]');if(anchor&&!anchor.getAttribute('href').startsWith('#/')){e.preventDefault();document.querySelector(anchor.getAttribute('href'))?.scrollIntoView();return}let b=e.target.closest('button');if(!b)return;let d=b.dataset;if(d.image){$('#image-dialog .zoom-content').innerHTML='<img src="images/'+d.image+'.png" alt="제품 원본 확대 이미지">';$('#image-dialog').showModal()}if(d.save){state.saved=state.saved.includes(d.save)?state.saved.filter(x=>x!==d.save):[...state.saved,d.save];persist();redraw();toast(state.saved.includes(d.save)?'취향에 저장했습니다.':'저장을 해제했습니다.')}if(d.size){chosen=d.size;redraw()}if(d.qty){qty=Math.max(1,Math.min(10,qty+Number(d.qty)));redraw()}if(d.cartQty){let[i,n]=d.cartQty.split(':');state.cart[i].qty=Math.max(1,Math.min(10,state.cart[i].qty+Number(n)));persist();redraw()}if(d.remove!==undefined){state.cart.splice(Number(d.remove),1);persist();redraw();toast('상품을 삭제했습니다.')}if(d.action?.startsWith('add:')){if(!chosen){$('#option-error').textContent='사이즈를 먼저 선택해 주세요.';toast('사이즈를 선택해 주세요.');return}let id=d.action.split(':')[1],old=state.cart.find(c=>c.id===id&&c.size===chosen);if(old)old.qty=Math.min(10,old.qty+qty);else state.cart.push({id,size:chosen,qty,selected:true});persist();redraw();toast('장바구니에 담았습니다. 오른쪽 위 가방에서 확인하세요.')}if(d.action==='checkout'){if(!state.cart.some(c=>c.selected!==false)){toast('주문할 상품을 선택해 주세요.');return}go('checkout')}if(d.action==='coupon'){state.coupon=true;persist();redraw();toast('웰컴 쿠폰을 받았습니다.')}if(d.action==='reset'){if(confirm('이 브라우저의 FORME 시안 기록을 모두 초기화할까요?')){localStorage.removeItem('forme-demo-v1');location.reload()}}});
document.addEventListener('change',e=>{let t=e.target,d=t.dataset;if(t.closest('#filters')){let params=new URLSearchParams(new FormData($('#filters'))),old=new URLSearchParams(currentRoute().split('?')[1]||'');if(old.get('q'))params.set('q',old.get('q'));go((currentRoute().startsWith('search')?'search':'shop')+'?'+params)}if(d.select!==undefined){state.cart[d.select].selected=t.checked;persist();redraw()}if(t.id==='select-all'){state.cart.forEach(c=>c.selected=t.checked);persist();redraw()}if(d.cartSize!==undefined){state.cart[d.cartSize].size=t.value;persist();redraw()}if(t.id==='apply-coupon'){couponOn=t.checked;$('#order-totals').innerHTML=sums(totals(state.cart.filter(c=>c.selected!==false),couponOn))}if(d.setting){state.settings[d.setting]=t.checked;persist();toast('설정을 저장했습니다.')}});
document.addEventListener('submit',e=>{const f=e.target;if(f.getAttribute('method')==='dialog')return;e.preventDefault();let data=new FormData(f);if(f.id==='search-form')go('search?q='+encodeURIComponent(data.get('q').trim()));if(f.id==='contact-form')go('done/contact');if(f.id==='review-form'){state.reviews.push({id:f.dataset.id,rating:Number(data.get('rating')),text:data.get('text')});persist();go('product/'+f.dataset.id);toast('시안 후기를 저장했습니다.')}if(f.id==='order-form'){let items=state.cart.filter(c=>c.selected!==false).map(c=>({...c}));if(!items.length)return;let id='FM-'+Date.now().toString(36).toUpperCase();state.orders.unshift({id,name:data.get('name'),date:new Date().toLocaleDateString('ko-KR'),items,totals:totals(items,couponOn)});state.cart=state.cart.filter(c=>c.selected===false);persist();couponOn=false;go('complete/'+id)}});
window.addEventListener('hashchange',()=>{if(!location.hash.startsWith('#/'))return;chosen='';qty=1;render();scrollTo(0,0)});render();


document.addEventListener('input',e=>{if(e.target.matches('#search-form input')){let el=document.getElementById('search-suggestions');if(!el){el=document.createElement('div');el.id='search-suggestions';el.setAttribute('aria-live','polite');document.getElementById('search-form').after(el)}let term=e.target.value.trim().toLowerCase();el.innerHTML=term?products.filter(p=>(p.name+' '+p.en+' '+p.color+' '+p.cat).toLowerCase().includes(term)).slice(0,4).map(p=>link('product/'+p.id,p.name+' ↗','under')).join('　')||'<p class="muted">일치하는 제품이 없습니다. 다른 검색어를 입력해 보세요.</p>':'';}});
